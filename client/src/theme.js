import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    pallete: {
        primary: {
            main: '#764abc'
        }
    },
    background: {
        default: '#7c1919'
    }
});

export default theme;