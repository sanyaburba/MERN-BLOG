import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    pallete: {
        primary: {
            main: '#764abc'
        }
    },
    background: {
        default: '#f7dfdf'
    }
});

export default theme;